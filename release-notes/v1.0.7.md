## 更新内容

- 修复 Nuitka 不设置 `sys.frozen` 导致打包程序无法正确启动 F3D 模型预览子进程的问题。
- 修复打包过程中 F3D 缺失时仍可能继续生成不完整发布包的问题。

## 构建与发布

- 构建时强制校验 `pyf3d.pyd`、`f3d.dll` 和 F3D 资源目录。
- 发布包生成后自动运行 F3D 导入与预览启动参数自检，自检失败时中止发布。
- README 明确区分可运行的 standalone 发布包与仅包含源码的 ZIP。
