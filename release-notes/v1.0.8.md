## 更新内容

- 文本、图片和文件详细信息预览改为非模态窗口，预览窗口保持打开或最小化时仍可继续操作主窗口。
- 再次打开同一服务上的同一文件时，自动恢复并激活已有的同类型预览窗口。
- 图片预览临时目录延迟到窗口关闭后清理，避免非模态窗口打开后资源被提前删除。

## 说明

- GLB/GLTF 模型预览仍由独立 F3D 子进程负责，本次未调整模型下载和进程管理流程。
- 文件预览下载仍在主线程执行，异步加载与取消操作留到后续阶段。
