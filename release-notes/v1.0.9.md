## 更新内容

- 文本、图片、GLB/GLTF 模型的下载与预览准备移到后台线程，加载期间主窗口保持可操作。
- 每个预览加载任务增加非模态进度窗口和取消操作。
- 同一文件正在加载时复用现有任务，避免重复下载。
- 跟踪 F3D 模型预览子进程，避免同一模型重复打开。
- 主程序退出时先取消预览加载任务，再关闭预览窗口、终止 F3D 子进程并清理临时目录。

## 说明

- 取消操作会在当前网络读取返回后生效，最长等待时间仍受现有 HTTP 超时设置影响。
- 已打开的 F3D 模型窗口由主程序统一管理；退出主程序会同时关闭这些模型预览进程。
